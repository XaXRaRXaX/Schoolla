function submitLogin() {
    const email = document.getElementById("email").value;
    const password = document.getElementById("password").value;
  
    // validate email and password format here, e.g. regex
  
    // if validation succeeds, perform login logic here
    // e.g. make a request to server to check if email and password match
  }
  